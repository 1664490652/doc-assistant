import os
from dotenv import load_dotenv

load_dotenv()

class Config:
    # ============================================================
    # LLM 主模型统一配置（兼容任意 OpenAI 协议提供方）
    # ------------------------------------------------------------
    # 优先级：LLM_API_KEY + LLM_BASE_URL（显式配置）
    #         > DEEPSEEK_API_KEY（向后兼容，自动补全 DeepSeek 端点）
    #         > OPENAI_API_KEY（默认 OpenAI 官方端点）
    # ============================================================
    OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")
    DEEPSEEK_API_KEY = os.getenv("DEEPSEEK_API_KEY")
    LLM_API_KEY = (
        os.getenv("LLM_API_KEY")
        or DEEPSEEK_API_KEY
        or OPENAI_API_KEY
    )
    LLM_BASE_URL = (
        os.getenv("LLM_BASE_URL")
        or ("https://api.deepseek.com/v1" if DEEPSEEK_API_KEY else None)
    )
    LLM_MODEL = os.getenv("LLM_MODEL", "deepseek-chat")
    LLM_TEMPERATURE = float(os.getenv("LLM_TEMPERATURE", "0.3"))

    # ============================================================
    # 备用模型配置（ModelFallback 中间件）
    # ------------------------------------------------------------
    # 默认走阿里云百炼 DashScope 原生协议（qwen-turbo）
    # 设置 FALLBACK_BASE_URL 后切换为任意 OpenAI 兼容提供方
    # ============================================================
    FALLBACK_API_KEY = os.getenv("FALLBACK_API_KEY") or os.getenv("DASHSCOPE_API_KEY")
    FALLBACK_BASE_URL = os.getenv("FALLBACK_BASE_URL")
    FALLBACK_MODEL = os.getenv("MIDDLEWARE_FALLBACK_MODEL", "qwen-turbo")

    # ============================================================
    # Embedding 配置（RAG 引擎）
    # ------------------------------------------------------------
    # 默认走阿里云百炼 DashScope（text-embedding-v4，国内直连）
    # 设置 EMBEDDING_BASE_URL 后切换为任意 OpenAI 兼容 Embedding
    # ============================================================
    EMBEDDING_API_KEY = os.getenv("EMBEDDING_API_KEY") or os.getenv("DASHSCOPE_API_KEY")
    EMBEDDING_BASE_URL = os.getenv("EMBEDDING_BASE_URL")
    EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "text-embedding-v4")

    ALLOWED_EXTENSIONS = {'.pdf', '.docx', '.doc', '.txt', '.xlsx', '.xls', '.csv', '.png', '.jpg', '.jpeg', '.bmp', '.gif', '.tiff'}

    # ============================================================
    # 工厂方法
    # ============================================================
    @classmethod
    def get_llm_config(cls):
        """返回统一 LLM 配置 (api_key, base_url, model)，供各模块复用。"""
        return cls.LLM_API_KEY, cls.LLM_BASE_URL, cls.LLM_MODEL

    @classmethod
    def build_chat_openai(cls, **overrides):
        """构建 ChatOpenAI 实例，统一从配置读取。
        适用于任意 OpenAI 协议兼容提供方。
        """
        if not cls.LLM_API_KEY:
            raise ValueError(
                "未配置 LLM API Key。请设置 LLM_API_KEY（或向后兼容的 "
                "DEEPSEEK_API_KEY / OPENAI_API_KEY）环境变量。"
            )
        from langchain_openai import ChatOpenAI
        kwargs = dict(
            model=cls.LLM_MODEL,
            api_key=cls.LLM_API_KEY,
            temperature=cls.LLM_TEMPERATURE,
        )
        if cls.LLM_BASE_URL:
            kwargs["base_url"] = cls.LLM_BASE_URL
        kwargs.update(overrides)
        return ChatOpenAI(**kwargs)
    
    # ========== 角色定义（System Prompt）==========
    SYSTEM_PROMPT = """
你是一名专业的办公文档分析专家，拥有丰富的文档处理经验。

**你的职责：**
- 快速理解各类办公文档（合同、报告、通知、证书、会议纪要等）
- 精准提取关键信息（条款内容、时间节点、任务要求、责任划分）
- 生成清晰、专业的结构化摘要
- 对比分析多文档内容差异与统一规范

**工作原则：**
1. 严谨细致：确保提取的信息准确无误
2. 条理清晰：输出内容结构分明，易于阅读
3. 专业简洁：语言专业但不晦涩，避免冗余
4. 客观中立：基于文档内容进行分析，不添加主观臆断

请始终保持专业、严谨的态度完成任务。
"""
    
    # ========== Few-shot 示例 ==========
    FEW_SHOT_EXAMPLES = """

参考以下示例的格式和风格进行输出：

【示例1 - 会议通知】
输入："关于召开2024年度总结会议的通知：各部门负责人请于2024年1月15日下午2:00到公司大会议室参加年度总结会议，请提前准备部门工作总结报告。"

输出：
## 📋 核心摘要
- 通知各部门负责人参加2024年度总结会议

## 📝 关键条款
- 参会人员：各部门负责人
- 需提前准备部门工作总结报告

## ⏰ 截止时间
- 2024年1月15日下午2:00

## ✅ 待办事项
- 各部门负责人参加年度总结会议
- 准备部门工作总结报告

## 📍 地点
- 公司大会议室

---

【示例2 - 合同条款】
输入："合同编号：HT-2024-001。甲方：XX科技有限公司，乙方：XX软件有限公司。合同约定：乙方需在2024年3月1日前交付定制开发的软件产品，逾期交付每天按合同金额的0.1%支付违约金。付款方式：合同签订后支付30%预付款，交付验收后支付60%，质保期结束支付10%。"

输出：
## 📋 核心摘要
- XX科技有限公司与XX软件有限公司签订软件定制开发合同

## 📝 关键条款
- 合同编号：HT-2024-001
- 付款方式：预付款30%，验收后60%，质保期结束10%
- 逾期交付违约金：每天按合同金额的0.1%支付

## ⏰ 截止时间
- 2024年3月1日前交付软件产品

## ⚠️ 注意事项
- 需按时交付，避免产生违约金

---

【示例3 - 证书】
输入："荣誉证书。第六届中国研究生人工智能创新大赛，华为杯。获奖作品：户外自主感知全向移动机器人。参赛单位：哈尔滨工程大学。参赛队员：房立安、田子湘、王炜。指导教师：王宇超。奖项：三等奖。颁发日期：2023年12月。"

输出：
## 📋 核心摘要
- 哈尔滨工程大学团队在第六届中国研究生人工智能创新大赛中获得三等奖

## 📝 关键信息
- 赛事名称：第六届中国研究生人工智能创新大赛（华为杯）
- 获奖作品：户外自主感知全向移动机器人
- 参赛单位：哈尔滨工程大学
- 参赛队员：房立安、田子湘、王炜
- 指导教师：王宇超
- 奖项等级：三等奖
- 颁发日期：2023年12月
"""
    
    # ========== 摘要生成提示词 ==========
    SUMMARY_PROMPT = SYSTEM_PROMPT + FEW_SHOT_EXAMPLES + """

请对以下办公文档进行智能摘要拆解：

文档内容：
{content}

请按照上述示例的格式输出，确保内容准确、结构清晰：

## 📋 核心摘要
- 用简洁的语言概括文档的主要内容和目的

## 📝 关键条款
- 列出文档中的重要条款、规定、政策要点

## ⏰ 截止时间
- 列出文档中提到的所有截止日期和时间节点

## ✅ 待办事项
- 列出需要执行的任务和行动项

## ⚠️ 注意事项
- 列出需要特别注意的事项和提醒

## 📍 其他信息
- 列出文档中的其他重要信息（如地点、人物、编号等）

请确保输出清晰、条理分明，语言简洁专业。
"""
    
    # ========== 并行化提取 Prompt（4 个独立任务）==========
    PARALLEL_PROMPTS = {
        "核心摘要": SYSTEM_PROMPT + """

请对以下办公文档，**仅提取核心摘要**，用简洁的语言概括文档的主要内容和目的。

文档内容：
{content}

输出格式：
## 📋 核心摘要
- 每条一个要点，简洁明了
""",
        "关键条款": SYSTEM_PROMPT + """

请对以下办公文档，**仅提取关键条款和规定**，列出文档中的重要条款、规定、政策要点。

文档内容：
{content}

输出格式：
## 📝 关键条款
- 每条一个条款，注明具体内容
""",
        "截止时间": SYSTEM_PROMPT + """

请对以下办公文档，**仅提取截止时间和时间节点**，列出文档中提到的所有时间相关信息。

文档内容：
{content}

输出格式：
## ⏰ 截止时间
- 每条一个时间节点，格式：YYYY年MM月DD日 + 具体事项
- 如果没有明确时间，请说明"文档中未提及明确时间"
""",
        "待办事项": SYSTEM_PROMPT + """

请对以下办公文档，**仅提取待办事项和行动项**，列出需要执行的任务和注意事项。

文档内容：
{content}

输出格式：
## ✅ 待办事项
- 每条一个任务，说明具体要做什么
## ⚠️ 注意事项
- 每条一个注意事项
""",
    }
    
    # ========== Chain of Thought（思维链）提示词 ==========
    COT_PROMPT = """
请按照以下步骤进行分析：
1. 首先识别文档类型（合同、报告、通知、证书、会议纪要等）
2. 提取文档中的关键要素（时间、地点、人物、事件、金额等）
3. 理解文档的核心目的和主要内容
4. 梳理需要特别关注的条款和事项
5. 按照要求的格式输出结构化结果
6. 检查输出是否完整、准确，是否符合预期格式
"""
    
    # ========== 评估器-优化器 Prompt ==========
    EVALUATOR_PROMPT = """
你是一名严格的文档校对专家。请逐条核对以下【提取结果】与【原始文档】中的事实是否一致。

重点关注以下类型的错误：
1. 数字错误：学号、编号、金额、日期、百分比等数字串是否精确匹配
2. 名称错误：人名、机构名、专有名词是否有增减字符
3. 遗漏：文档中明确提到的信息是否在提取结果中缺失
4. 编造：提取结果中是否出现了文档中不存在的信息

**重要约束（防止 OCR 噪声污染）：**
- 学号、编号、卡号、金额、电话、日期等结构化字段，其值只能由字母、数字、
  以及 - / . : 等分隔符组成，绝不能包含汉字或其他非法字符。
- 若【原始文档】因 OCR 识别错误，在上述结构化字段中混入了汉字/乱码，
  请以"干净的字母数字串"为准，**不要**把噪声字符当作正确值要求补回。
- 仅当你高度确信提取结果确实写错时才报告该字段；存疑时一律视为无错误。

原始文档：
{content}

提取结果：
{summary}

请以 JSON 格式返回，不要包含其他内容：
{{
  "has_errors": true/false,
  "errors": [
    {{"field": "字段名", "wrong": "错误值", "correct": "正确值", "reason": "错误原因"}}
  ]
}}

如果没有任何错误，返回：{{"has_errors": false, "errors": []}}
"""
    
    OPTIMIZER_PROMPT = """
你是一名专业的文档分析专家。你之前提取的文档信息存在以下错误，请根据【原始文档】修正。

原始文档：
{content}

之前的提取结果：
{summary}

评估器发现的错误：
{errors}

请直接输出修正后的完整提取结果，注意：
- 禁止输出 JSON、错误清单、任何诊断信息
- 禁止以"好的"、"根据您"等对话式语句开头
- 直接以【核心摘要】或 ## 标题开始正文
"""

    # ── 对比分析专用评估器-优化器 Prompt ──
    COMPARE_EVALUATOR_PROMPT = """
你是一名严格的文档校对专家。请逐条核对以下【对比分析结果】与各【原始文档】中的事实是否一致。

重点关注以下类型的错误：
1. 跨文档引用错误：A文档的信息被错误归到B文档
2. 数字/日期错误：各文档中的编号、金额、日期是否精确
3. 结论与文档证据矛盾：分析结论是否与文档原文一致
4. 遗漏：某文档中明确的重要差异点是否在对比中缺失
5. 编造：对比结果中是否出现了任何文档中都不存在的信息

原始文档内容：
{content}

对比分析结果：
{summary}

请以 JSON 格式返回，不要包含其他内容：
{{
  "has_errors": true/false,
  "errors": [
    {{"field": "字段名", "wrong": "错误值", "correct": "正确值", "reason": "错误原因"}}
  ]
}}

如果没有任何错误，返回：{{"has_errors": false, "errors": []}}
"""

    COMPARE_OPTIMIZER_PROMPT = """
你是一名专业的文档分析专家。你之前的文档对比分析存在以下错误，请根据各【原始文档】修正。

原始文档内容：
{content}

之前的对比分析结果：
{summary}

评估器发现的错误：
{errors}

请直接输出修正后的完整对比分析结果，注意：
- 禁止输出 JSON、错误清单、任何诊断信息
- 禁止以"好的"、"根据您"等对话式语句开头
- 保持原有的对比分析结构和章节标题
"""
    
    # ========== 文档类型特定提示词 ==========
    DOC_TYPE_PROMPTS = {
        "合同": "重点关注：合同双方信息、标的内容、履行期限、付款方式、违约责任、争议解决方式",
        "报告": "重点关注：核心数据、结论建议、问题分析、解决方案、实施计划",
        "通知": "重点关注：发布单位、通知对象、时间地点、具体要求、联系人信息",
        "证书": "重点关注：证书名称、获得者信息、颁发机构、获奖等级、有效期",
        "会议纪要": "重点关注：会议主题、参会人员、讨论内容、决议事项、待办任务",
        "邮件": "重点关注：发件人、收件人、主题、核心内容、附件说明、行动要求",
        "规章制度": "重点关注：适用范围、条款内容、责任划分、生效日期、解释权归属"
    }
    
    # ========== 结构化输出格式说明 ==========
    OUTPUT_FORMAT_GUIDE = """
输出格式要求：
1. 使用Markdown格式，标题使用## 开头
2. 列表项使用 - 开头，每项为一个完整的要点
3. 内容要简洁明了，避免冗长
4. 时间格式统一为：YYYY年MM月DD日 HH:MM
5. 金额格式统一为：人民币XX元（或XX元）
6. 人员姓名使用全称，避免简称
"""
    
    # ========== 文档对比提示词 ==========
    COMPARE_PROMPT = SYSTEM_PROMPT + """

请对比以下多个办公文档，分析它们的内容差异和统一规范：

文档列表：
{documents}

分析步骤：
1. 分别理解每个文档的核心内容和目的
2. 识别各文档之间的相同点和不同点
3. 梳理共同遵循的规范和标准
4. 分析差异可能带来的影响和风险
5. 提出合理的整合建议

请按照以下格式输出分析结果：

## 🔍 内容差异分析
- 列出各文档之间的主要差异点，包括时间、条款、责任等方面的不同

## 📊 统一规范梳理
- 列出各文档中一致的内容和规范
- 识别共同遵循的原则和标准

## 💡 建议整合方案
- 提出整合多个文档的建议方案
- 指出需要协调统一的事项

## ⚠️ 风险提示
- 指出文档差异可能带来的风险和问题

请确保分析全面、客观，输出结构清晰、易于理解。
"""
